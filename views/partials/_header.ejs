<header
  class="bg-white flex items-center px-8 pt-4 justify-between max-w-screen-2xl mx-auto"
>
  <!-- Logotype -->
  <a href="/">
    <img src="/images/logotype.png" alt="Logo" class="max-w-52" />
  </a>
  <div class="flex flex-row">
    <!-- Menu -->
    <nav class="hidden lg:block">
      <div class="flex space-x-8 ml-auto">
        <a href="#" class="uppercase font-extrabold hover:underline"
          >Bästsäljare</a
        >
        <a href="#" class="uppercase font-extrabold hover:underline">Nyheter</a>
        <a href="#" class="uppercase font-extrabold hover:underline"
          >Inspiration</a
        >
        <a href="/product-list" class="uppercase font-extrabold hover:underline"
          >Alla posters</a
        >
      </div>
    </nav>

    <!-- Search, Cart, and Dropdown Icon -->
    <div class="flex items-center space-x-6 ml-6">
      <button>
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="currentColor"
          class="w-6"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M15.75 10.5V6a3.75 3.75 0 1 0-7.5 0v4.5m11.356-1.993 1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 0 1-1.12-1.243l1.264-12A1.125 1.125 0 0 1 5.513 7.5h12.974c.576 0 1.059.435 1.119 1.007ZM8.625 10.5a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Zm7.5 0a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Z"
          />
        </svg>
      </button>

      <button id="searchBtn">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="currentColor"
          class="w-6"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="m21 21-5.197-5.197m0 0A7.5 7.5 0 1 0 5.196 5.196a7.5 7.5 0 0 0 10.607 10.607Z"
          />
        </svg>
      </button>

      <!-- Dropdown Icon -->
      <div class="dropdown-icon relative lg:hidden">
        <button
          onclick="toggleDropdown()"
          class="hover:bg-gray-200 p-2 rounded-full"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
            class="size-6"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"
            />
          </svg>
        </button>

        <!-- Dropdown Menu -->
        <div
          id="dropdownMenu"
          class="hidden absolute right-0 mt-2 w-48 bg-white shadow-lg rounded-lg"
        >
          <a
            href="#"
            class="flex flex-col px-6 py-4 text-gray-800 hover:bg-gray-100"
            >Bästsäljare</a
          >
          <a
            href="#"
            class="flex flex-col px-6 py-4 text-gray-800 hover:bg-gray-100"
            >Nyheter</a
          >
          <a
            href="#"
            class="flex flex-col px-6 py-4 text-gray-800 hover:bg-gray-100"
            >Inspiration</a
          >
          <a
            href="#"
            class="flex flex-col px-6 py-4 text-gray-800 hover:bg-gray-100"
            >Alla kategorier</a
          >
        </div>
      </div>
    </div>
  </div>
</header>

<!-- Search Modal -->
<div id="searchModal" class="hidden">
  <div
    class="fixed inset-0 bg-black bg-opacity-50 flex justify-center items-center z-50"
  >
    <div class="bg-white p-6 rounded-lg w-11/12 sm:w-1/3 relative">
      <button id="closeBtn" class="absolute top-2 right-2 text-xl">
        &times;
      </button>
      <h2 class="font-bold text-lg mb-4">Hitta din nästa favorit</h2>
      <input
        type="text"
        id="searchInput"
        class="w-full p-2 border border-gray-300 rounded"
        placeholder="Skriv för att söka..."
      />
      <div id="searchResults" class="mt-4"></div>
      <!-- Här kommer sökresultaten att visas -->
    </div>
  </div>
</div>

<script>
  // Hämtar element från DOM
  var modal = document.getElementById("searchModal");
  var btn = document.getElementById("searchBtn");
  var span = document.getElementById("closeBtn");
  var searchInput = document.getElementById("searchInput");

  // Visa modal när användaren klickar på Sök-knappen
  btn.onclick = function (event) {
    event.preventDefault(); // Förhindra omdirigering direkt
    var query = searchInput.value;

    if (query) {
      // Om det finns ett sökord, omdirigera direkt till sökresultat
      window.location.href = `/search?q=${query}`;
    } else {
      // Annars öppna modalen för att skriva en sökterm
      modal.classList.remove("hidden");
    }
  };

  // Stäng modal när användaren klickar på kryssknappen
  span.onclick = function () {
    modal.classList.add("hidden");
  };

  // Stäng modal om användaren klickar utanför den
  window.onclick = function (event) {
    if (event.target == modal) {
      modal.classList.add("hidden");
    }
  };

  // Vid "Enter"-tryck på sökfältet, omdirigera till sökresultat
  searchInput.addEventListener("keypress", function (e) {
    if (e.key === "Enter") {
      var query = searchInput.value;
      if (query) {
        // Se till att det finns ett sökord
        window.location.href = `/search?q=${query}`; // Ompeka till search.ejs med sökterm
      }
    }
  });
</script>
<script src="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"></script>
