<!DOCTYPE html>
<html>
  <head>
    <title><%= product.name %></title>
    <link href="/styles/output.css" rel="stylesheet" />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;600&family=Jaro&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <%- include('partials/_header') %>
    <main
      class="flex flex-col gap-10 md:flex-row w-full py-16 px-8 md:px-20 max-w-screen-2xl mx-auto md:items-center"
    >
      <!-- Produktbild -->
      <img
        src="/images/poster-images/<%= product.image %>"
        alt="Poster av <%= product.name %>"
        class="w-full md:w-1/3 rounded-lg"
      />
      <!-- Produktinformation -->
      <div class="flex flex-col gap-8 items-center md:items-start">
        <h1 class="text-3xl font-semibold"><%= product.name %></h1>
        <p class="text-lg text-gray-600">
          <%= product.description %>
          <!-- Dynamiskt hämtad beskrivning -->
        </p>
        <span class="text-xl font-bold text-black">
          <%= product.price %> SEK
          <!-- Dynamiskt hämtat pris -->
        </span>
        <button class="primary-button md:self-start mt-4">
          Lägg till i varukorgen
        </button>
      </div>
    </main>

    <%- include('partials/_footer') %>
    <div class="fixed top-5 right-5 bg-black text-white p-4 rounded shadow-lg flex items-center gap-2 z-[1000] opacity-0 invisible transition-opacity duration-300" id="notification">
      <span id="notification-text"></span>
      <a href="/shoppingCart" id="view-cart" class="text-blue-500 no-underline hover:underline">View Cart</a>
      <button class="bg-none border-none text-white text-base cursor-pointer" id="close-notification">×</button>
    </div>
    <script src="/javascript/product-details.js"></script>
  </body>
</html>
